[tool.poetry]
name = "remerge-mwe"
version = "0.2.0"
description = "REMERGE is a Multi-Word Expression (MWE) discovery algorithm derived from the MERGE algorithm."
authors = ["Peter Baumgartner <5107405+pmbaumgartner@users.noreply.github.com>"]
readme = "README.md"
packages = [{include = "remerge", from = "src"}]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"
tqdm = "^4.64.1"
numpy = ">=1.18,<1.23"


[tool.poetry.group.dev.dependencies]
pytest = "^7.1.3"
ipython = "^8.5.0"
black = "^22.8.0"
flake8 = "^5.0.4"
py-spy = "^0.3.14"
pytest-sugar = "^0.9.5"
pytest-cov = "^4.0.0"
mypy = "^0.982"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
